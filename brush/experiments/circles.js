function buildCirclesIncrement(incrementX, incrementY) {
  colors = {
    background: color("#0000ff"),
    // accent: color("#000000"),
    // detail: color("#ffffff"),
  };

  const shapes = [backgroundShape(colors.background)];
  return shapes;
}
